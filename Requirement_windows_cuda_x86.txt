# --- 指定 PyTorch 的 CUDA 官方源 (针对 RTX 5080 推荐 CUDA 12.4) ---
--extra-index-url https://download.pytorch.org/whl/cu124

# --- 核心数值计算 ---
numpy==1.26.4

# --- AI 与多媒体处理 ---
# Windows 上直接用标准版 OpenCV
opencv-python
moviepy
Pillow
scikit-learn
# PyTorch 组件 (会从上面的 index-url 下载 CUDA 版)
torch
torchaudio
# Whisper 依赖
openai-whisper
numba
mediapipe
demucs
scenedetect[opencv]

# --- AI API 与 NLP ---
dashscope
jieba
openai
transformers

# --- 任务队列与后端 ---
# 注意：Celery 在 Windows 上不支持 prefork 模式，启动需加 -P solo
celery
redis
# Windows 也可以使用 gevent 来改善 Celery并发 (可选)
gevent 

# --- Web Server / Deployment ---
fastapi
# gunicorn 已移除 (仅限 Linux/Mac)，Windows 使用 uvicorn
uvicorn[standard]
python-multipart

# --- 其他网络与数据库 ---
requests
pymongo
